/*
import { gql } from "apollo-server-express";

export const typeDefs = gql`
  type Dish {
    name: String
    serving: Int
    ingredients: [Ingredient]
  }

  type Ingredient {
    name: String
    quantity: Int
  }

  type Query {
    dishes: [Dish]
    ingredients: [Ingredient]
  }
`;
*/
